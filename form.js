import React from "react"

// 関数型コンポーネント
export  const Form = () => {
    return (
        <form>
            <input type="text"/>
        </form>
    );
}

//export default Form;

// exportの注意 : default の場合は、あとで描く
// exportの注意 : defaultなし の場合は、最初に書く
